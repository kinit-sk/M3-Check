run_args:
  experiment_name: llama_transformer[text]
  description:

modalities:
  posts:
    image:
      all_dummy: true
    ocr:
      all_dummy: true

model:
  _target_: models.RetrievalLammaTransformer
  config:
    _target_: models.make_llama_config
    hidden_size: ${args.emb_dim}
    intermediate_size: 64
    num_attention_heads: 4
    num_hidden_layers: 3
